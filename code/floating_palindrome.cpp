#include<bits/stdc++.h>
#include<string.h>
using namespace std;
bool isPalindrome(float m)
{
	
}
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	float n;
	cin>>n;
	if(isPalindrome(n))
	{
		cout<<"Yes"<<endl;
	}
	else
	{
		cout<<"No"<<endl;
	}
}
